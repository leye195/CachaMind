/*import { handleNotification } from "./chat";
const socket = io("/");
const sendMessage = msg => {
  socket.emit("newMsg", { message: msg });
  console.log(`You : ${msg}`);
};
const setNickName = name => {
  socket.emit("setNickName", { nickname: name });
};

socket.on("msgNotify", handleNotification);*/
